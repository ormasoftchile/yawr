apiVersion: yawr.runbook/v1
id: enum-warn-fixture
name: enum-warn-fixture
inputs:
  env_name:
    type: string
    required: true
    enum: ["Prod", "prod"]
flow:
  - step:
      id: end
      type: end
      outcome: {category: success, code: ok}
