flow:
  - step:
      type: assert
      assert:
        - type: matches
          subject: '${vars.name}'
          expected: &db_name '^[A-Za-z0-9][A-Za-z0-9-]{0,62}$'
        - type: matches
          subject: '${vars.other}'
          expected: *db_name
        - type: eq
          subject: '${regex.match(vars.name, "^ab+$")}'
          expected: 'true'
        - type: eq
          subject: '^ab+$'
          expected: '^ab+$'
