// Package parser implements the runbook parser.
// It reads YAML source, validates structure against the JSON Schema,
// and produces ParsedRunbook values for downstream consumption by the Planner.
package parser
