apiVersion: yawr.runbook/v1

id: edge-case-single-step-timeout
name: edge-case-single-step-timeout
kind: reference
description: Edge case runbook with exactly 1 step that waits for a timeout window

flow:
  - step:
      id: wait_timeout
      type: noop
      title: Wait for timeout window
      delay: 5s
