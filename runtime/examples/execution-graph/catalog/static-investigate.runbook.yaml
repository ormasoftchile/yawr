apiVersion: yawr.runbook/v1
id: graph-static-investigate
name: Static investigation child
flow:
  - step: {id: collect, type: noop}
  - step:
      id: classify
      type: include
      include:
        runbook: classify.runbook.yaml
  - step: {id: child_return, type: noop}
