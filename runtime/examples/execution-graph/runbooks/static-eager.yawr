apiVersion: yawr.runbook/v1
id: graph-static-eager
name: Static eager include
flow:
  - step: {id: before, type: noop}
  - step:
      id: child
      type: include
      include:
        runbook: ../catalog/static-investigate.yawr
        expand: eager
  - step: {id: after, type: noop}
